<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Purfle.App.Pages.SearchPage"
             Title="Search Marketplace">

    <Grid RowDefinitions="Auto,*" Padding="16">

        <SearchBar x:Name="SearchBar"
                   Placeholder="Search agents..."
                   SearchButtonPressed="OnSearch" />

        <CollectionView x:Name="ResultsView"
                        Grid.Row="1"
                        SelectionMode="Single"
                        SelectionChanged="OnAgentSelected">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="8" ColumnDefinitions="*,Auto" RowDefinitions="Auto,Auto">
                        <Label Text="{Binding Name}"
                               FontAttributes="Bold"
                               FontSize="16" />
                        <Label Text="{Binding LatestVersion}"
                               Grid.Column="1"
                               TextColor="Gray"
                               FontSize="14" />
                        <Label Text="{Binding Description}"
                               Grid.Row="1"
                               Grid.ColumnSpan="2"
                               TextColor="Gray"
                               FontSize="13"
                               MaxLines="2"
                               LineBreakMode="TailTruncation" />
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>

            <CollectionView.EmptyView>
                <Label Text="No agents found in the marketplace."
                       HorizontalOptions="Center"
                       VerticalOptions="Center"
                       TextColor="Gray" />
            </CollectionView.EmptyView>
        </CollectionView>

    </Grid>

</ContentPage>
