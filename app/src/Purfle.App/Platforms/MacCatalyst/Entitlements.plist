<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
    <dict>
        <!-- App Sandbox — required for Mac App Store distribution. -->
        <key>com.apple.security.app-sandbox</key>
        <true/>

        <!-- Outbound network connections (API calls to Azure, Anthropic, etc.). -->
        <key>com.apple.security.network.client</key>
        <true/>

        <!-- Keychain access group — credential storage for agent API keys. -->
        <key>keychain-access-groups</key>
        <array>
            <string>$(AppIdentifierPrefix)PLACEHOLDER_BUNDLE_ID</string>
        </array>

        <!-- User notifications — agent completion / error toasts. -->
        <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
        <false/>
    </dict>
</plist>
